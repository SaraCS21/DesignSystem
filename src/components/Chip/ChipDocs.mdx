{/* ChipDocs.mdx */}

import { Meta, Primary, Controls } from '@storybook/blocks';
import * as ChipStories from './Chip.stories.ts';

<Meta of={ChipStories} />

# Chip

`my-chip` es un componente de chip personalizable construido utilizando `LitElement`.

<Primary />

Este componente permite ajustar diversas propiedades como el color o el texto que contiene.

## Props

<Controls />

## Example Usage

```html
<my-chip variant="contained" color="blue"> Example Chip </my-chip>
```
